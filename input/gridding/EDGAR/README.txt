Directory to place raw EDGAR grids

Grids should be stored inside a directory that matches there EDGAR version. 
Inside that should be a directory for each emission species, which will contain the 
zipped nc files by sector.